/**
 * Defining the alert objects to be used with cars.
 * @author Matthew Gimbut
 */

/**
 * Constructor for Alert objects
 * @param priority The priority of the alert
 * @param message The message contained in the alert
 * @constructor
 */
function Alert(priority, message) {
    this.priority = priority;
    this.message = message;
}